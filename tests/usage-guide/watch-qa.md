# 关注操作流畅度验证（2026-09-22）

发布目录：`outputs/关键词排名每日跟进网页版-v2.1-关注流畅版-20260922-正式版`

实现：即时更新星标；后台串行保存；Worker 执行 IndexedDB 数据读取、写入和每日备份；保存返回关注记录，不重建完整产品数据。保留行顺序与排名历史，失败回滚到最近一次已保存状态。

验证命令：

```powershell
node tests/usage-guide/watch-qa.cjs outputs/关键词排名每日跟进网页版-v2.1-关注流畅版-20260922-正式版
npm run verify:web -- --dir outputs/关键词排名每日跟进网页版-v2.1-关注流畅版-20260922-正式版
```

在隔离的 Edge headless 浏览器中通过：

- 点击不显示全屏加载，行位置保持不变。
- 连续关注/取消/关注以最后一次操作为准，刷新后保留。
- 注入实际 Worker 启动失败，界面回滚、持久化数据不变。
- 关注和标注交错保存均不丢失，排名历史 SHA256 不变。
- 保存中切换商品，两个商品分别写入各自 ASIN，不切回之前的商品。
- 主数据与每日备份的关注列表一致。
- 自然/SP 矩阵状态一致；搜索条件和水平/垂直滚动位置保留。
- 滚动测试先对星标中心进行 elementFromPoint 命中检查，再用鼠标坐标点击，避免自动化 locator 自动滚动干扰结果。
- 页面错误为 0，实际保存中截图已检查，主界面保持可操作。

最后一次单次样本：后台保存 277.7 ms，最大动画帧间隔 60 ms，超过 50 ms 的帧为 1。此为本机种子数据样本，不作为所有设备、数据规模或云端环境的性能保证。旧版样本曾出现整页遮罩与完整数据回传；不将非同条件旧样本作为严格性能倍数比较。

截图与运行结果：`work/watch-qa-20260922/`。云端持久化分支保留原写入方式，本次端到端测试覆盖本地网页版。
